---
title: "マイグレーションガイド"
---

<!-- toc -->

# マイグレーションガイド

このガイドは、既存のスクリプトを新しいAPIバージョンに追従させるための、書き換えの手引きです。

---

このガイドは、APIバージョンごとに節を設け、その世代で導入された破壊的変更ごとに、既存の記法やイディオムをどのように書き換えればよいかを説明します。

---

## APIバージョン5

### `IN` の挙動の変更

APIバージョン5では、 `IN` が標準入力全体を1個の文字列として読み取るようになりました。

標準入力を1行ずつのストリームとして読み取るには、 `IN` を `INL` に書き換えてください。
