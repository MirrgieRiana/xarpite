Added support for comparing `BOOLEAN` values using the spaceship operator `<=>`.
Added support for comparing `NULL` with any value using the spaceship operator `<=>`, where `NULL` is ordered before other values.
