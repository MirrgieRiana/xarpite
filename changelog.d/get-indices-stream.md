Added support for passing a stream of indices to the `GET` function, returning the corresponding elements as a stream.
Made the return value of the `GET` function a non-stream when `indices` is a non-stream.
