Added the `XARPITE_STACK_SIZE_KB` environment variable that configures the native engine's stack size limit (256 MiB by default) to mitigate process crashes from deeply recursive regular expressions.
